# Minimal scaffolding dependencies only.
# Phase-specific deps (spaCy, scikit-learn, DB drivers, etc.) will be added
# by their owning phases per PROJECT_SPEC.md. Do not add extras without approval.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
# Phase 2: required by FastAPI for multipart/form-data uploads (POST /api/upload).
python-multipart>=0.0.18,<1
# Phase 3: initial NLP framework (extraction uses spaCy NER + deterministic
# rules; en_core_web_sm is fetched separately, see Dockerfile).
spacy>=3.7,<4
# Phase 4: official Neo4j driver (graph writes/reads; unit tests use a fake).
neo4j>=5,<6
# Phase 5: Isolation Forest anomaly detection (deterministic random_state).
scikit-learn>=1.3,<2
# Phase 6: local JWT authentication (no external auth service).
PyJWT>=2.8,<3
